// Klassen IdeellFörening som ärver klassen Förening genom nyckelordet extends
public class IdeellForening extends Forening                                
{
    // Definierar attributen för klassen IdeellFörening och även egenskaperna namn och adress från klassen Förening
    private int antalMedlemmar;                                             
    private String namn;         
    private String adress;

    public IdeellForening(String namn, String adress, int antalMedlemmar)
    {
        super(namn, adress);                  // Anropar superklassens konstruktor, egenskaperna/attributen namn och adress
        this.antalMedlemmar=antalMedlemmar;   // Tilldelar antalMedlemmar parametern till klassvariablen genom att skriva this framför
    }
    public void setAntalMedlemmar()
    {
         this.antalMedlemmar=antalMedlemmar;
    }
    public int getAntalMedlemmar()
    {
        return antalMedlemmar;
    }
    public String toString()
    {
        return "Antalet medlemmar är:  " + antalMedlemmar + " och adress" + adress + super.toString();
    }
}

